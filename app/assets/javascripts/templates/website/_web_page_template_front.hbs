<div class="card-container front">

  {{ partial "website/webPageTemplateHead" }}

  <div class="card-body">
    <ul class="widgets widgets--large">
      {{#each mainWidgets}}
        {{ partial "website/widget" }}
      {{/each}}
    </ul>
    <div class="buttons">
      {{#if isWebHomeTemplate}}
        {{#linkTo "webHomeTemplate" model.website model class="btn btn--small"}}Edit{{/linkTo}}
      {{else}}
        {{#linkTo "webPageTemplate" model.website model class="btn btn--small"}}Edit{{/linkTo}}
      {{/if}}
      <a {{bindAttr href="previewUrl"}} class="btn btn--small">Preview</a>
    </div>

    {{ partial "website/webPageTemplateFlip" }}
  </div>
</div>