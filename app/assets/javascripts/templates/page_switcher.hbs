{{#view App.ToggleBtn}}
  <div id="page-switch" class="switch switch-small" data-on="success" data-off="danger">
    <input type="checkbox"
      id="page-{{!<%= web_template.id %>}}"
      {{!<%= "checked" unless web_template.disabled? %>}}
    />
  </div>
{{/view}}