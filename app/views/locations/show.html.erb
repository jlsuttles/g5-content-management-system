<% content_for :head do
     javascript_tag("$(function(){ new window.toggleSwitch($('.page-actions .switch')); });")
   end %>

<div class="page-title">
  <h2>
    <%= "Location: #{@location.name}" %>
    <%= link_to "Create New Page", new_location_web_page_template_path(@location), class: 'btn btn-large primary new-page' %>
  </h2>
</div>

<table class='pages table table-striped'>
  <thead>
    <tr>
      <th>Page</th>
      <th>Layout</th>
      <th>Theme</th>
      <th>Widgets</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <%= render partial: "web_template", locals: { location: @location, web_template: @location.website_template } %>
    <% @location.web_page_templates.order("created_at ASC").each do |web_page_template| %>
      <%= render partial: "web_template", locals: { location: @location, web_template: web_page_template } %>
    <% end %>
  </tbody>
</table>

<div id="modal" class="modal hide fade" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
	<div class="modal-header">
		<button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
		<h3 id="myModalLabel">Edit Widget</h3>
	</div>
	<div class="modal-body">
	</div>
</div>
