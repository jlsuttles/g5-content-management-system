<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf8" />
    <title>G5 Client Hub: <%= @client_name %> - <%= content_for?(:title) ? yield(:title) : "Untitled" %></title>
    <link href='http://fonts.googleapis.com/css?family=Oswald' rel='stylesheet' type='text/css'>
    <%= stylesheet_link_tag "application" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tag %>
    <%= yield(:head) %>
  </head>
  <body>
    <div class="container">

      <header role="banner">
        <h1 class="brand"><a href="<%= root_path %>"><span class="logo">G5</span> Client Hub: <strong><%= @client_name %></strong></a></h1>

        <nav role="navigation">
          <ul>
            <%= content_tag :li, link_to("Locations", locations_path) %>
            <%= content_tag :li, link_to("Leads", ENV["LEADS_SERVICE_HEROKU_URL"]) %>
            <%= content_tag :li, link_to("Siblings", siblings_path) %>
            <%= content_tag :li, link_to("Resque", "/resque", target: "_blank") %>
          </ul>
        </nav>
      </header>

      <% flash.each do |name, msg| %>
        <%= flash_div name %>
      <% end %>

      <section role="main">

        <!-- <div class="row page-header">
          <%= content_tag :h2, yield(:title), class: "span10" if show_title? %>
          <%= content_tag :span, yield(:header_right), class: "span2" if show_header_right? %>
        </div> -->

        <%= yield %>

      </section>

    </div>
  </body>
</html>
