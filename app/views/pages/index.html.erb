<% title "Page" %>

<table>
  <tr>
    <th>Name</th>
  </tr>
  <% for pages in @pages %>
    <tr>
      <td><%= pages.name %></td>
      <td><%= link_to "Show", pages %></td>
    </tr>
  <% end %>
</table>

<p><%= link_to "New Page", new_page_path %></p>
